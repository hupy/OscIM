$(document).ready(() => {
    const store = new Storage()
    
    loadExtension()
    function loadExtension() {
        const $html = $('html')
        const $document = $(document)
        layui.layim.config({
            init: {
            
            }
        })
    }
})